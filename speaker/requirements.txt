# torch (CPU) est installé à part dans le Dockerfile (index PyTorch CPU) pour
# éviter la variante CUDA (plusieurs Go). Ne pas ajouter torch ici.
fastapi==0.115.6
uvicorn[standard]==0.32.1
resemblyzer==0.1.4
numpy<2
