# ── Sentinel : configuration ─────────────────────────────────────────────
# Copiez ce fichier en `.env` puis remplissez au minimum ANTHROPIC_API_KEY.
# Ne committez JAMAIS le fichier .env (il est ignoré par git).

# ── Obligatoire ──────────────────────────────────────────────────────────
# Clé API Anthropic — https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ── LLM ──────────────────────────────────────────────────────────────────
# Modèle du cerveau. Alternatives : claude-opus-5 (plus capable, plus cher),
# claude-haiku-4-5 (économique). Voir docs/ARCHITECTURE.md.
SENTINEL_MODEL=claude-sonnet-5
# Longueur max. d'une réponse (les réponses vocales sont volontairement courtes)
SENTINEL_MAX_TOKENS=1024
# Profondeur de réflexion : low | medium | high (low = latence et coût minimaux,
# adapté à la conversation courante ; montez pour des réponses plus travaillées)
SENTINEL_EFFORT=low
# Nombre de messages d'historique envoyés au modèle à chaque tour
SENTINEL_HISTORY_WINDOW=30

# ── Réseau ───────────────────────────────────────────────────────────────
# Port de l'interface web (HTTPS) sur le LAN
SENTINEL_PORT=8443
# HTTPS : on = certificat auto-signé généré automatiquement (ou vos propres
# certificats déposés dans data/certs/). off = HTTP simple, UNIQUEMENT derrière
# un reverse proxy qui gère lui-même le TLS (le micro exige un contexte sécurisé).
SENTINEL_TLS=on

# ── Voix ─────────────────────────────────────────────────────────────────
# Modèle faster-whisper : small-int8 (défaut CPU) | medium (GPU conseillé) | tiny-int8…
WHISPER_MODEL=small-int8
# Voix Piper française : fr_FR-siwis-medium (défaut) | fr_FR-upmc-medium | …
PIPER_VOICE=fr_FR-siwis-medium

# ── Divers ───────────────────────────────────────────────────────────────
TZ=Europe/Paris
LOG_LEVEL=INFO

# ── Home Assistant (Nova) ────────────────────────────────────────────────
# URL de Nova, ex. http://192.168.1.10:8123 — et jeton longue durée (compte admin)
HA_URL=
HA_TOKEN=

# ── Atelier de développement (Phase 3B) ──────────────────────────────────
# Authentification de Claude Code dans le worker. DEUX options :
#  1. RECOMMANDÉ (couvert par ton forfait Claude Pro/Max, pas de crédits API) :
#     sur ton PC où Claude Code est connecté, lance `claude setup-token` et
#     colle le jeton généré ici.
#  2. Sinon, laisser vide : le worker utilisera ANTHROPIC_API_KEY (crédits API).
CLAUDE_CODE_OAUTH_TOKEN=
# Jeton GitHub (PAT, portée « repo » sur Alardware/*) pour pouvoir POUSSER les
# branches proposées. Vide = les diffs restent consultables mais pas de push.
GITHUB_TOKEN=
# Dépôts autorisés pour les tâches de dev (liste blanche, séparés par espaces)
DEV_REPOS=https://github.com/Alardware/atrium https://github.com/Alardware/loggia
# Durée maximale d'une tâche de dev (secondes)
DEV_TASK_TIMEOUT=1800

# ── Surveillance (Phase 3A) ──────────────────────────────────────────────
# URL d'Atrium pour le contrôle de santé HTTP (vide = non surveillé)
ATRIUM_URL=
# Heure locale du rapport quotidien, format HH:MM (vide = pas de rapport)
SENTINEL_DAILY_REPORT=07:30
# Seuil mémoire d'un conteneur avant signalement dans les audits (en Mo)
SENTINEL_CONTAINER_MEM_MO=1500
